# Example configuration — copy to a local path and set CONFIG_PATH, or override any field via environment.
# Do not commit real credentials. Env vars override these values when set (see README).

database_driver: sqlite
database_url: "file:access.db?_pragma=foreign_keys(1)"
http_addr: "127.0.0.1:8080"
migrations_dir: migrations/sqlite

# Graceful shutdown timeout (seconds) after SIGINT/SIGTERM
shutdown_timeout_seconds: 30

# Optional: require Authorization: Bearer <token> on /api/v1/*. Env API_BEARER_TOKEN overrides when set.
# Leave empty for local loopback-only dev; do not commit real secrets.
# api_bearer_token: ""

# Optional: comma-separated list of allowed CORS origins. Env CORS_ALLOWED_ORIGINS overrides when set.
# Default (*) reflects the actual request Origin header so credentials work in browsers.
# Use an explicit list (e.g. https://app.example.com) before exposing the service beyond loopback.
# Set to "none" to disable CORS response headers entirely (e.g. when a reverse proxy manages CORS).
# cors_allowed_origins: "*"

# --- PostgreSQL example ---
# database_driver: postgres
# database_url: "postgres://user:password@localhost:5432/access_manager?sslmode=disable"
# migrations_dir: migrations/postgres

# --- MySQL / MariaDB example ---
# database_driver: mysql
# database_url: "user:password@tcp(localhost:3306)/access_manager?parseTime=true"
# migrations_dir: migrations/mysql
